# Webapp config (optional)
# Path to a CRNet .pth weights file.
# Default (if unset): webapp/weights/ACS-WFC-BN-16.pth
# Examples:
#   CRNET_MODEL_PATH=webapp/weights/ACS-WFC-BN-16.pth
#   CRNET_MODEL_PATH=webapp/weights/ACS-WFC-AttBN-16.pth
CRNET_MODEL_PATH=webapp/weights/ACS-WFC-BN-16.pth

# CPU or GPU (GPU requires CUDA + a CUDA build of PyTorch)
CRNET_DEVICE=CPU

# UNet hidden channels used in training (usually 32)
CRNET_HIDDEN=32

# (Optional) number of Torch threads on CPU
CRNET_TORCH_THREADS=1

# Optional explicit UNet config overrides (only needed if your weight filename doesn't include hints)
# CRNET_ATT=true
# CRNET_ACT_NORM=batch   # batch | group | none
# CRNET_DEEPER=false
